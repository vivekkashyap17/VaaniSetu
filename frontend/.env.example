# Copy to .env. Vite only exposes vars prefixed with VITE_ to the browser,
# so the API key here is visible to clients — use a low-risk/demo key.
VITE_API_BASE_URL=http://localhost:8000
VITE_API_KEY=change-me

# VaaniSetu backend — runtime dependencies only (CPU inference).
# This is a curated DIRECT-dependency list, not a `pip freeze` dump: transitive
# deps (scipy, scikit-learn, tokenizers helpers, indicnlp fork, etc.) resolve
# automatically. Notably absent on purpose: bitsandbytes (GPU-only, unused),
# accelerate (optional extra, unused), and all Sphinx/doc tooling — see the
# Dockerfile cleanup step which also strips the doc stack pulled in transitively.

# --- Web framework / server ---
fastapi==0.136.1
uvicorn==0.46.0
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2

# --- Rate limiting ---
slowapi==0.1.9

# --- Persistence ---
SQLAlchemy==2.0.49

# --- ML / inference (CPU) ---
torch==2.11.0+cpu
transformers==4.41.2
tokenizers==0.19.1
safetensors==0.7.0
sentence-transformers==5.5.1
faiss-cpu==1.13.2
sentencepiece==0.2.1
sacremoses==0.1.1
numpy==2.4.4

# --- Hugging Face hub (model download) ---
huggingface_hub==0.36.2
hf-xet==1.5.0

# --- Language detection / Indic NLP ---
langdetect==1.0.9
indic_transliteration==2.3.82
IndicTransToolkit==1.1.1

# --- HTTP client used in the pipeline ---
requests==2.34.0
